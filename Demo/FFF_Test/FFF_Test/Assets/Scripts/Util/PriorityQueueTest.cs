using UnityEngine;
using System.Collections;

public class PriorityQueueTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
		/*
		PriorityQueue<int> pq = new PriorityQueue<int>();

		string unsorted = "Unsorted:";
		int randNum;
		for (int i = 0; i < 10; i++) {
			randNum = Random.Range(0, 100);
			pq.Insert(randNum);
			unsorted += " " + randNum;
		}
		Debug.Log(unsorted);

		// Print the numbers in order from priority queue
		string sorted = "Sorted:";
		for (int i = 0; i < 10; i++) {
			sorted += " " + pq.Pop();
		}
		Debug.Log(sorted);
		*/

	}

}
